Gem::Specification.new do |spec|
  spec.name = "watch-parse-markdown-scope"
  spec.version = "0.1.0"
  spec.summary = "Implement a Ruby parsers project for markdown incremental indexing, using append-only fixtures and checkpoint recovery checks."
  spec.files = Dir["lib/**/*.rb"]
  spec.required_ruby_version = ">= 3.0"
end
